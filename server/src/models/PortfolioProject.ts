import mongoose, { type Model } from 'mongoose'; const { Schema, model, models } = mongoose;
interface Doc{repoName:string;featured?:boolean;displayTitle?:string;shortDescription?:string;role?:string;image?:string;priority?:number;technologyOverrides?:string[];hidden:boolean;relatedRepositories?:{label:string;name?:string;url:string}[]}
const relatedRepositorySchema = new Schema({ label: { type: String, required: true, trim: true, maxlength: 40 }, name: { type: String, trim: true, maxlength: 100 }, url: { type: String, required: true, trim: true } }, { _id: false });
const schema=new Schema<Doc>({repoName:{type:String,required:true,unique:true,trim:true},featured:{type:Boolean},displayTitle:{type:String,trim:true,maxlength:100},shortDescription:{type:String,trim:true,maxlength:300},role:{type:String,trim:true,maxlength:100},image:{type:String,trim:true},priority:{type:Number},technologyOverrides:[{type:String,trim:true}],hidden:{type:Boolean,default:false},relatedRepositories:[relatedRepositorySchema]},{timestamps:true});
export const PortfolioProject=(models.PortfolioProject as Model<Doc>)??model<Doc>('PortfolioProject',schema);
